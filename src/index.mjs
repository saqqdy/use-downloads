import useDownloads from './index.cjs.js'

const { version, download, downloadUrlFile, openFile, saveFile } = useDownloads

export { useDownloads as default, version, download, downloadUrlFile, openFile, saveFile }
